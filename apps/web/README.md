# UniLink web application

The Next.js application contains localized discovery pages, university and
program APIs, MapLibre exploration, Auth.js, Prisma-backed registration, and
the student onboarding flow.

Run commands from the repository root. See the root `README.md` for setup,
environment variables, database commands, and the architecture boundary.
